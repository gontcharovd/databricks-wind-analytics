# The main job for project.
resources:
  jobs:
    project_job:
      name: project_job

      trigger:
        # Run this job every day, exactly one day from the last run; see https://docs.databricks.com/api/workspace/jobs/create#trigger
        periodic:
          interval: 1
          unit: DAYS

      tasks:
        - task_key: notebook_task
          notebook_task:
            notebook_path: ../src/notebook.ipynb
        
        - task_key: refresh_pipeline
          depends_on:
            - task_key: notebook_task
          pipeline_task:
            pipeline_id: ${resources.pipelines.project_pipeline.id}
        
        # - task_key: main_task
        #   environment_key: default
        #   depends_on:
        #     - task_key: refresh_pipeline
        #   python_wheel_task:
        #     package_name: project
        #     entry_point: main
        #   libraries:
        #     # By default we just include the .whl file generated for the project package.
        #     # See https://docs.databricks.com/dev-tools/bundles/library-dependencies.html
        #     # for more information on how to add other libraries.
        #     - whl: ../dist/*.whl

      # Configure serverless compute
      environments:
        - environment_key: default
          spec:
            client: '1'
            dependencies:
              - /Workspace/Users/denis@gontcharov.eu/.bundle/project/dev/artifacts/.internal/project-*.whl